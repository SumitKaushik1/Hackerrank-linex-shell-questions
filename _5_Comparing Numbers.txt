read x;
read y;
# you can use double parenthesiisis it supports the < some exprssionse
if ((x < y)) 
then echo "X is less than Y";
elif ((x>y)) 
then echo "X is greater than Y";
else 
echo "X is equal to Y";
fi;
